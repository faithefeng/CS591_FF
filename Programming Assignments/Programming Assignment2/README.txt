Step1 : Install required packages
pip install twitter
pip install termcolore
pip install json
pip install colorama

Step2: go to the project folder and run the script
python ProgrammingAssignment2.py

Then the results are stored in file--result.txt and result.json